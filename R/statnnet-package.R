#' statnnet: statistical summaries for fitted nnet models
#'
#' `statnnet` provides regression-style statistical summaries for small,
#' parsimonious, single-hidden-layer neural networks already fitted with
#' [nnet::nnet()]. It does not refit, select, or provide post-selection
#' inference for neural networks.
#'
#' @keywords internal
"_PACKAGE"
